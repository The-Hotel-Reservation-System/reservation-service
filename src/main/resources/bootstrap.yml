spring:
  application:
    name: reservation-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: root
      fail-fast: true
      import:
        optional:
          configserver: instead